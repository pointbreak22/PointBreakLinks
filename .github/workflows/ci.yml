name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  backend:
    name: Backend (build + test)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: Api/PointbreakLinksApi
    steps:
      - uses: actions/checkout@v7

      - name: Setup .NET
        uses: actions/setup-dotnet@v6
        with:
          dotnet-version: '10.0.x'

      - name: Restore
        run: dotnet restore PointbreakLinksApi.slnx

      - name: Build
        run: dotnet build PointbreakLinksApi.slnx --no-restore --configuration Release

      # Application.Tests mocks every repository/service dependency (Moq) — no database or
      # other external service needed to run it, see PROJECT_MAP.md's "Что уже готово".
      - name: Unit tests
        run: dotnet test Application.Tests/Application.Tests.csproj --no-build --configuration Release --logger "console;verbosity=normal"

      # Boots the real app via WebApplicationFactory against a real Postgres in a Testcontainers
      # container — ubuntu-latest ships Docker preinstalled, no extra setup needed. This is the
      # suite that caught the Jwt:Secret eager-vs-lazy-read bug (see PROJECT_MAP.md).
      - name: Integration tests
        run: dotnet test WebAPI.IntegrationTests/WebAPI.IntegrationTests.csproj --no-build --configuration Release --logger "console;verbosity=normal"

  frontend:
    name: Frontend (build + test)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: Client/pointbreak-links-client
    steps:
      - uses: actions/checkout@v7

      - name: Setup Node
        uses: actions/setup-node@v7
        with:
          node-version: '24'
          cache: 'npm'
          cache-dependency-path: Client/pointbreak-links-client/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Type-check
        run: npx tsc --noEmit

      - name: Lint
        run: npx ng lint

      # @angular/build:unit-test runs on Vitest (jsdom), not Karma/Chrome — no browser needed.
      - name: Unit tests
        run: npx ng test --watch=false

      - name: Production build
        run: npx ng build --configuration production
